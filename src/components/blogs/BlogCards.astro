---

import ProjectCard from './BlogCard.astro';

const { allProjects } = Astro.props;

---
{allProjects.map(p => 
  <ProjectCard
    title={p.title} 
    subTitle={p.subTitle}
    link={p.url}
    noOfLines={p.lines}    
  />
)}
